<%= form_with(model: [:users_backoffice, tweet], local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class='control'>
      <%= form.text_area :declaration, class:'textarea', placeholder: 'What are you thinking ?' %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit 'Publish', class:"button is-link is-small" %><br />
  </div>
<% end %>
